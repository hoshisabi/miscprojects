[project]
name = "miscprojects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "groq>=1.1.1",
    "pydub>=0.25.1",
    "python-dotenv>=1.2.2",
]

[tool.uv.workspace]
members = [
    "ancient_nations",
]
